

{% block body %}


    {{ form(edit_form) }}
    {{ dump(edit_form.username.vars) }}
    {#{{ form_errors(edit_form.username) }}#}
    {#&#123;&#35;{{ form_widget(edit_form.email) }}&#35;&#125;#}
    {{ form_errors(edit_form.referral) }}
    {{ form_errors(edit_form.campus) }}

    {{ edit_form.vars.errors|length }}

    {% if edit_form.username.vars.errors|length > 0 %}
        <ul class="form-errors name">
            {% for error in edit_form.username.vars.errors %}
                {#{{ error }}#}
            {% endfor %}
        </ul>
    {% endif %}

{% endblock %}


