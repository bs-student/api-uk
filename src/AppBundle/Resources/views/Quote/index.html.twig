{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Quote list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quotetype</th>
                <th>Quotedescription</th>
                <th>Quoteimage</th>
                <th>Quoteprovider</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('quote_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quoteType }}</td>
                <td>{{ entity.quoteDescription }}</td>
                <td>{{ entity.quoteImage }}</td>
                <td>{{ entity.quoteProvider }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('quote_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('quote_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('quote_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
