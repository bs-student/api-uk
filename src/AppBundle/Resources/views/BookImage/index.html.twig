{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BookImage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imagename</th>
                <th>Imageurl</th>
                <th>Titleimage</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('bookimage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.imageName }}</td>
                <td>{{ entity.imageUrl }}</td>
                <td>{{ entity.titleImage }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('bookimage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('bookimage_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('bookimage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
